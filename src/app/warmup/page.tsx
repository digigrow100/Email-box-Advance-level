import {AppShell}from"@/components/app-shell";import{PageHeader}from"@/components/page-header";import{RampControls}from"@/components/ramp-controls";import{listMailboxes}from"@/lib/data/repository";
export default async function WarmupPage(){const boxes=await listMailboxes();return <AppShell><PageHeader title="Sending ramp" description="Increase real sending volume gradually while keeping per-mailbox limits under control."/><div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-950"><strong>Safe warm-up:</strong> MailPilot controls volume and timing. It does not generate fake opens or automated conversations between accounts to game spam filters.</div><div className="card overflow-hidden"><div className="responsive-table"><table className="data-table"><thead><tr><th>Mailbox</th><th>Ramp day</th><th>Current limit</th><th>Sent today</th><th>Controls</th></tr></thead><tbody>{boxes.map(m=><tr key={m.id}><td><div className="font-semibold">{m.email}</div><div className="mt-1 text-xs capitalize text-slate-400">{m.status}</div></td><td>Day {m.rampDay}</td><td>{m.dailyLimit}/day</td><td>{m.sentToday}</td><td><RampControls mailboxId={m.id} target={Math.max(m.dailyLimit,30)}/></td></tr>)}</tbody></table></div></div></AppShell>}
