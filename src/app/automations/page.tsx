import Link from "next/link";import {AppShell}from"@/components/app-shell";import{PageHeader}from"@/components/page-header";import{AutomationForm}from"@/components/automation-form";import{listAutomationRules}from"@/lib/data/repository";
export default async function AutomationsPage(){const rules=await listAutomationRules();return <AppShell><PageHeader title="Automations" description="Trigger actions when new mail arrives. AI drafts are the safest default."/><div className="grid gap-6 xl:grid-cols-[1fr_1.2fr]"><section className="card overflow-hidden"><div className="border-b border-slate-100 p-5"><h2 className="font-bold">Active rules</h2></div><div className="divide-y divide-slate-100">{rules.map((r:any)=><div key={r.id} className="p-5"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold">{r.name}</p><p className="mt-1 text-xs text-slate-500">{r.trigger_type} · priority {r.priority}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${r.enabled?"bg-emerald-50 text-emerald-700":"bg-slate-100 text-slate-500"}`}>{r.enabled?"Enabled":"Off"}</span></div><p className="mt-3 text-xs leading-5 text-slate-500">Conditions: {JSON.stringify(r.conditions)}<br/>Actions: {JSON.stringify(r.actions)}</p></div>)}{!rules.length&&<p className="p-6 text-sm text-slate-500">No rules yet.</p>}</div></section><AutomationForm/></div></AppShell>}
